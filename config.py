from math import sin, pi

v_max = 2
m = 192
m_sh = 1

T = 30
energy = sum(range(0, T))

prirost_koef = sin(-pi/2 + (pi * (T + 0.5 + Oxi))/40)

O2 = 10

def v(w, m):
    return v_max * (w / 80) * (200 / m)

